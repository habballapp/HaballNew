package com.example.haball.Distributor.ui;

public interface IOnBackPressed {
    /**
     * If you return true the back press will not be taken into account, otherwise the activity will act naturally
     */
    void onBackPressed();
}